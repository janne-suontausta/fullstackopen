const Statistics = (props) => {
    return (
      <div>
        <p>Good {props.good}</p>
        <p>Good {props.neutral}</p>
        <p>Good {props.bad}</p>
      </div>
    )
  }

  export default Statistics;